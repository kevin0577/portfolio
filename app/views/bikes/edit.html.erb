<%= render 'layouts/errors', obj: @bike %>

<div class="row">
	<div class="col-xs-offset-3"><h2 class="title col-xs-3">投稿編集画面</h2></div>
</div><br>

<div class="postedit">
<%= form_for(@bike) do |f| %>
<div class="row">
	<div class="postimage">
            <%= f.attachment_field :image, class: "col-xs-offset-2"%><br>

		<div class="field">
			<%= f.text_area :title, placeholder: "内容", size: "50x10" %>
		</div><br>

		<div class="check_box">
			<p>ジャンル</p>
			<%= f.collection_check_boxes(:tag_ids, Tag.all, :id, :tag_name) do |t| %>
				<div>
				<%= t.label do %>
				<%= t.check_box %>
				<%= t.text %>
				<% end %>
				</div>
			<% end %>
		</div>
		<br>

		<div class="actions">
			<%= f.submit "変更を保存", class: "btn btn-primary col-xs-4 col-xs-offset-4" %>
		</div>
	</div>
</div>
<% end %>
</div>